# =====================================================
# TCDAFS Backend Configuration Template - Version 2.0
# Updated for revised database schema (2025-11-10)
# =====================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env in the backend folder:
#    Windows: copy backend\.env.txt backend\.env
#    Linux/Mac: cp backend/.env.txt backend/.env
# 2. Update the values below with your actual credentials
# 3. NEVER commit the .env file to version control
#
# =====================================================

# =====================================================
# DATABASE CONFIGURATION - SUPABASE (REQUIRED)
# =====================================================
# Using Supabase session pooler (port 6543) for better SQLAlchemy compatibility with SSL
# Replace with your actual Supabase credentials

DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require
DB_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require

# Example (DO NOT USE IN PRODUCTION):
# DATABASE_URL=postgresql://postgres.aanjnzqymddrgqfblhve:NIBM%402025adds@aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require
# Note: URL-encode special characters in password (@ becomes %40, # becomes %23, etc.)

# =====================================================
# CSV FALLBACK (DEPRECATED - Schema v2.0 is database-only)
# =====================================================
# CSV fallback is no longer supported in schema v2.0
# The system now operates exclusively with PostgreSQL database
USE_CSV_FALLBACK=false

# =====================================================
# JWT AUTHENTICATION CONFIGURATION (REQUIRED)
# =====================================================
# Generate a secure SECRET_KEY for production:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

SECRET_KEY=your-secret-key-change-this-to-random-32-plus-chars-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Production example (GENERATE YOUR OWN):
# SECRET_KEY=Kx8mP3vN9qRt2wY5zAc7dF0gHj4kLn6pQs8uVx1yZb3eIm5oRu7wY0zA2cF4gH6j

# =====================================================
# APPLICATION SETTINGS
# =====================================================
# Set DEBUG=false in production for security
DEBUG=true
LOG_LEVEL=INFO

# =====================================================
# CORS CONFIGURATION
# =====================================================
# Comma-separated list of allowed origins
# Development (includes Dash and React defaults):
CORS_ORIGINS=http://localhost:8050,http://127.0.0.1:8050,http://localhost:3000,http://127.0.0.1:3000

# Production (replace with your actual domain):
# CORS_ORIGINS=https://your-production-domain.com,https://dashboard.your-domain.com

# =====================================================
# DATA DIRECTORY (OPTIONAL - For future exports)
# =====================================================
# Path to data files directory (relative to backend folder)
DATA_DIR=../data

# =====================================================
# ML MODEL ARTIFACTS (OPTIONAL - For future features)
# =====================================================
# Path to trained model file (relative to backend folder)
MODEL_PATH=../model_artifacts/best_model.joblib

# =====================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =====================================================
# Before deploying to production, ensure you have:
#
# ✅ Changed SECRET_KEY to a secure random string
# ✅ Set DEBUG=false
# ✅ Set LOG_LEVEL=WARNING or ERROR
# ✅ Updated CORS_ORIGINS to your production domain(s)
# ✅ Using strong database password
# ✅ Enabled SSL for database connection (sslmode=require)
# ✅ Verified all credentials are correct
# ✅ Added .env to .gitignore
#
# =====================================================
# SCHEMA VERSION INFORMATION
# =====================================================
# This configuration is for Database Schema v2.0
# Updated: 2025-11-10
# Changes from v1.0:
# - Removed CSV fallback support
# - Simplified field names
# - Enhanced capacity tracking
# - PostgreSQL-only operation
# =====================================================
